# Muse (Meta). `muse login` stores a CLI credential; the API needs a key.
META_API_KEY=
LLM_BASE_URL=https://api.meta.ai/v1
LLM_MODEL=muse-spark-1.3

# Elasticsearch. Cloud or local, anything that speaks the ES REST API.
ELASTIC_URL=
ELASTIC_API_KEY=
