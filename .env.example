# --- TypeScript app (the submitted one) ---

# Muse Spark (Meta). `muse login` stores a credential in your keychain.
META_API_KEY=
LLM_BASE_URL=https://api.meta.ai/v1
LLM_MODEL=muse-spark-1.3

# Elasticsearch. Cross-thread search, and sessions that survive a restart.
ELASTIC_URL=
ELASTIC_API_KEY=

# --- Python implementation in insights/, which speaks the same contract ---

ELASTIC_URL=
ELASTIC_API_KEY=
ANTHROPIC_API_KEY=
